---
title: "独立性"
type: concept
chapter: ch01
tags:
  - 概率论与统计
  - 独立性
  - 随机变量
sources: [茆诗松]
date: 2026-04-18
related:
  - "[[1.5 独立性]]"
  - "[[3.2 边际分布与随机变量的独立性]]"
  - "[[条件概率与条件期望]]"
  - "[[第一章 随机事件与概率 — 章节汇总]]"
  - "[[随机变量]]"
---

# 独立性

> [!abstract] 概述
> 独立性（Independence）是概率论中最核心的概念之一。它描述了两个或多个随机事件、随机变量之间"互不影响"的关系，是大数定律、中心极限定理等极限定理的核心前提条件。

---

## 一、随机事件的独立性

### 两个事件的独立性

> [!def] 事件独立
> 设 $A, B$ 为同一概率空间的两个事件，若
> $$P(AB) = P(A)P(B)$$
> 则称 $A$ 与 $B$ **相互独立**。

> [!tip] 等价条件
> 若 $P(B) > 0$，则 $A$ 与 $B$ 独立 $\Leftrightarrow P(A|B) = P(A)$

### 多个事件的独立性

> [!def] $n$ 个事件相互独立
> 称事件 $A_1, A_2, \cdots, A_n$ **相互独立**，若对任意子集 $\{i_1, \cdots, i_k\}$（$k \geqslant 2$）有：
> $$P(A_{i_1} A_{i_2} \cdots A_{i_k}) = P(A_{i_1})P(A_{i_2})\cdots P(A_{i_k})$$

---

## 二、随机变量的独立性

### 定义

> [!def] 随机变量独立
> 设 $X_1, X_2, \cdots, X_n$ 为随机变量，若对任意 $x_1, \cdots, x_n \in \mathbb{R}$ 有：
> $$F_{X_1,\cdots,X_n}(x_1, \cdots, x_n) = F_{X_1}(x_1) \cdots F_{X_n}(x_n)$$
> 则称 $X_1, \cdots, X_n$ **相互独立**。

### 离散型独立的等价条件

$$P(X_1 = x_1, \cdots, X_n = x_n) = P(X_1 = x_1) \cdots P(X_n = x_n)$$

### 连续型独立的等价条件

$$f_{X_1,\cdots,X_n}(x_1, \cdots, x_n) = f_{X_1}(x_1) \cdots f_{X_n}(x_n)$$

---

## 三、独立性的判别方法

### 方法一：分布函数法

检查联合分布函数是否等于边际分布函数的乘积。

### 方法二：概率密度/质量函数法

离散型：检查联合 PMF 是否等于各 PMF 的乘积。

连续型：检查联合 PDF 是否等于各 PDF 的乘积。

### 方法三：不相关性检验

对于**二维正态分布**，不相关 $\Leftrightarrow$ 独立。

但注意：**一般情况下，不相关 ≠ 独立**。

---

## 四、独立性的运算性质

| 运算 | 性质 |
|------|------|
| 若 $A \perp B$，则 $A \perp B^c$ | ✅ |
| 若 $A \perp B$，则 $A^c \perp B^c$ | ✅ |
| 若 $A \perp B$ 且 $A \perp C$，且 $B \cap C = \varnothing$，则 $A \perp (B \cup C)$ | ✅ |
| 独立随机变量的函数彼此独立 | ✅ |
| 独立随机变量之和的分布 = 卷积 | ✅ |

---

## 五、"两两独立" ≠ "相互独立"

> [!warning] 常见误区
> "两两独立"（每对事件独立）并不意味着"相互独立"（所有事件联合独立）。

**典型例子**：投掷两个骰子，定义事件：
- $A_1$ = "第一个骰子为奇数"
- $A_2$ = "第二个骰子为奇数"  
- $A_3$ = "两个骰子之和为奇数"

可以验证：$A_1, A_2, A_3$ 两两独立，但 $P(A_1A_2A_3) \neq P(A_1)P(A_2)P(A_3)$。

---

## 六、相关章节

- [[第一章 随机事件与概率 — 章节汇总]]
- [[1.4 条件概率]] — 独立性与条件概率的关系
- [[随机变量]] — 独立随机变量的定义
- [[协方差与相关系数]] — 不相关与独立的关系
- [[大数定律]] — 独立性的核心应用
